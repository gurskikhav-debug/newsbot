name: 🤖 Telegram Бот (с меню и поиском)

on:
  workflow_dispatch:
    inputs:
      command:
        description: 'Команда для бота (например: /start или /search)'
        required: true
        default: '/start'
      keywords:
        description: 'Ключевые слова для поиска (если /search)'
        required: false
        default: ''

jobs:
  run_bot:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Получить код
        uses: actions/checkout@v4

      - name: 🐍 Установить Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Установить зависимости
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔐 Восстановить кеш новостей
        uses: actions/cache@v3
        id: cache-news
        with:
          path: cache/news_cache.json
          key: news-cache-v1

      - name: 📁 Создать папку cache
        run: mkdir -p cache

      - name: 📄 Инициализировать кеш (если отсутствует)
        if: steps.cache-news.outputs.cache-hit != 'true'
        run: echo "[]" > cache/news_cache.json

      - name: 🏃 Запустить бота
        env:
          TOKEN: ${{ secrets.TOKEN }}
          NEWSAPI_KEY: ${{ secrets.NEWSAPI_KEY }}
          ADMIN_ID: ${{ secrets.ADMIN_ID }}
          GITHUB_ACTIONS: "true"
          BOT_COMMAND: ${{ github.event.inputs.command }}
          KEYWORDS: ${{ github.event.inputs.keywords }}
        run: python bot.py

      - name: 💾 Сохранить обновлённый кеш
        uses: actions/cache@v3
        with:
          path: cache/news_cache.json
          key: news-cache-v1
